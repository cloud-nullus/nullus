# Nullus 마크

세잎 매듭(trefoil). 세 가닥이 서로를 지나며 하나로 이어진다 —
쿠버네티스 · 인프라 · 애플리케이션이 따로 놀지 않고 한 흐름으로 묶인다는 뜻이다.

## 단일 출처

도형을 손으로 그리지 않는다. 매듭은 눈으로 그리면 반드시 틀린다 —
어느 가닥이 위인지가 세 교차점에서 일관되지 않으면 매듭이 아니라 겹친 선이 된다.
`emit.mjs` 가 매개변수식에서 점을 뽑고 자기교차점을 수치로 찾아 산출물을 굽는다.

```
x = sin t + 2 sin 2t
y = cos t − 2 cos 2t
```

산출물은 **두 개**이고, 둘 다 이 스크립트가 만든다.

| 산출물 | 쓰이는 곳 |
|---|---|
| `web/public/favicon.svg` | 브라우저 탭 · 북마크. 번들을 타지 않는 정적 파일 |
| `web/src/components/brand/mark-geometry.generated.ts` | `<NullusMark>` 가 읽는 경로·색 데이터 |

도형을 고쳤을 때만 다시 굽는다. 앱은 런타임에 이 스크립트를 실행하지 않는다.

```sh
node docs/40_UI_UX/logo/emit.mjs \
  web/public/favicon.svg \
  web/src/components/brand/mark-geometry.generated.ts
```

둘이 갈라지면 `web/src/components/brand/nullus-mark.test.tsx` 가 잡는다 —
favicon 이 컴포넌트와 같은 경로를 담고 있는지 대조한다. 한쪽만 다시 굽고
커밋하는 사고를 막기 위한 것이다.

브랜드 자산으로 SVG 파일이 필요하면 `web/public/favicon.svg` 를 그대로 쓴다.
사본을 docs 에 두지 않는다 — 대조하는 테스트가 없는 사본은 조용히 낡는다.

## 교차 간격

아래로 지나가는 가닥은 **끊어서** 위아래를 표현한다. 끊는 자리는 위 가닥과
같은 경로를 굵게(`MARK_STROKE + MARK_GAP * 2`) 그은 마스크다.
간격을 좌표로 따로 계산하지 않는 이유는, 그렇게 하면 굵기를 바꿀 때마다
간격이 위 가닥과 어긋나기 때문이다. 같은 경로에서 파생시키면 언제나 나란하다.

`GAP` 은 16px 에서 간격이 뭉개지지 않는 하한으로 잡았다. 사이드바 마크가
20px 이고 파비콘은 16px 까지 내려간다 — 그 크기에서 매듭으로 읽히지 않으면
로고로서 실패한 것이다.

## 색

색은 매듭을 따라 흐른다. 세 지점을 잡고 그 사이를 48조각으로 보간한다.

| 뜻 | 값 |
|---|---|
| 쿠버네티스 | `#326ce5` |
| 인프라 | `#6b3fd4` |
| 애플리케이션 | `#12b0a0` |

이 값들은 `web/DESIGN.md` 토큰이 **아니다**. 로고는 테마를 따라가면 안 된다 —
라이트에서 파랑이고 다크에서 회색인 마크는 로고가 아니라 아이콘이다.
그래서 `src/components/brand/**` 는 hex 금지 규칙에서 면제돼 있다
(`web/eslint.config.js`). 색이 정해진 바탕 위에 얹어야 할 때만
`<NullusMark tone="current">` 로 단색 한 붓이 되어 글자색을 물려받는다.

### 어두운 바탕용 두 번째 벌

색상은 바꾸지 않되 **밝기만 올린 벌**을 함께 굽는다. HSL 의 lightness 는
사람이 느끼는 밝기가 아니라서, 파랑과 보라 사이를 HSL 로 이으면 중간이 양 끝보다
어두운 남색으로 꺼진다 — 휘도로 보면 `.176 → .071 → .118`. 밝은 바탕에서는 그
구간이 오히려 또렷하지만, 어두운 바탕에서는 대비가 2.1:1 까지 떨어져 매듭의
그쪽 절반이 배경에 잠긴다. 청록 쪽은 7.3:1 이라 한 마크 안에서 3.5배가 벌어졌다.

다크 벌은 목표 휘도에 못 미치는 색만 lightness 를 이분법으로 올려
`--color-surface-base` 기준 **4.5:1** 을 보장한다(편차 1.81배). 색상·채도는
건드리지 않으므로 파랑은 파랑, 보라는 보라로 남는다 — 실제로 파랑과 청록은
거의 그대로이고 보라만 `#6b3fd4` → `#8663dc` 로 올라간다.

바탕을 판별하는 신호는 두 곳이 다르다.

- **앱**: `theme-store` 를 읽는다. 사용자가 OS 와 무관하게 테마를 고를 수 있으므로
  `prefers-color-scheme` 을 쓰면 강제로 라이트를 켠 사용자에게 어긋난다.
- **파비콘**: React 밖의 독립 문서라 스토어가 닿지 않는다. 브라우저 크롬은 OS
  설정을 따르므로 여기서는 `prefers-color-scheme` 이 옳다 — SVG 안에 `<style>`
  로 넣었고, CSS 가 presentation attribute 를 이기므로 기본값은 속성에 두고
  다크만 덮는다.

대비 약속은 `nullus-mark.test.tsx` 가 `tokens.generated.css` 의 실제 바탕
토큰을 읽어 검사한다. 바탕이 밝아지면 마크만 조용히 과하게 밝은 채로 남기 때문이다.
