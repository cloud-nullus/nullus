#cloud-config
# Kubernetes/containerd/CNI는 Kubespray에서 설치한다.
ssh_pwauth: false
disable_root: true
package_update: true
package_upgrade: false
packages:
  - python3
  - python3-apt
  - sudo
  - ca-certificates
  - curl
